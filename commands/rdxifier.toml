# rdxifier slash command
# Activates unified caveman + ponytail mode
# Usage: /rdx [lite|full|ultra]

[command]
name = "rdx"
description = "Activate rdxifier mode: caveman prose compression + YAGNI ladder code decisions. Levels: lite, full (default), ultra."
argument-hint = "[lite|full|ultra]"

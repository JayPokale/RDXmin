# rdx-review — review a diff/file for over-engineering
[command]
name = "rdx-review"
description = "Review the current diff or a file for bloat, speculative abstractions, and reinvented stdlib."
argument-hint = "[file or empty for current diff]"

# rdx-audit — one-shot efficiency audit across both axes
[command]
name = "rdx-audit"
description = "Audit a diff, file, or repo for BOTH over-engineered code and bloated prose/docs. Ranked report, biggest cut first. Changes nothing."
argument-hint = "[path | repo | empty for current diff]"
